name: Build Check
on:
  push:
    branches:
      - main
jobs:
  build-check:
    runs-on: ubuntu-latest
    steps:
      - name: Get repository code
        uses: actions/checkout@v3
      - name: Install deps
        run: yarn install
      - name: Lint
        run: yarn lint
      - name: Build
        run: yarn build